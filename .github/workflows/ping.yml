name: Keep Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: curl --fail --silent --show-error --retry 5 --retry-delay 5 --retry-all-errors https://browser-to-pdf.onrender.com/health
